[package]
name = "services"
version = "0.1.0"
edition = "2024"

[dependencies]
modules = { path = "../modules" }
repositories = { path = "../repositories" }

anyhow = "1"
async-trait = "0.1"
tracing = "0.1"
tokio = { version = "1", features = ["full"] }
